import { defineConfig } from 'astro/config';

export default defineConfig({
  site: 'https://gogoulet.github.io',
  base: '/dev_ares', // Pas de point, juste le nom du repo encadré par des slashes
});
