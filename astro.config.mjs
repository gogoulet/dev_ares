import { defineConfig } from 'astro/config';

export default defineConfig({
  // Ton domaine GitHub Pages principal
  site: 'https://gogoulet.github.io',
  // Le nom exact de ton dépôt GitHub (très important !)
  base: '/dev_ares',
});
